import { BorderCaderDirective } from './border-cader.directive';

describe('BorderCaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderCaderDirective();
    expect(directive).toBeTruthy();
  });
});
